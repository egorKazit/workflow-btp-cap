-- generated by cds-compiler version 3.3.2
VIEW localized_sap_workflow_schema_Item AS SELECT
  L.ID,
  L.name,
  L.header_ID
FROM sap_workflow_schema_Item AS L
WITH ASSOCIATIONS (
  MANY TO ONE JOIN localized_sap_workflow_schema_Header AS header ON (header.ID = header_ID)
)