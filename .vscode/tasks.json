{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build workflow-java",
      "type": "shell",
      "command": "mvn clean install",
      "options": {
        "cwd": "${workspaceFolder}/workflow-java"
      }
    }
  ]
}